
The continuume membrane model aims to simulate the spherical membrane with two monolayers. It allows to set up the locally finer mesh.
To run the simulation:
g++ continuummodel_maincode_thickness_sphere.cpp -o job1 -fopenmp
./job1 > out& 
